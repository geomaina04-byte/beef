services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: beeftrace-web:latest
    container_name: beeftrace-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
